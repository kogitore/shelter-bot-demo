# Telegram Bot Configuration
# 從 @BotFather 取得你的 Bot Token
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

# Database Configuration
# Neon PostgreSQL 連線字串
# 格式: postgresql://[username]:[password]@[host]/[database]?sslmode=require
# 從 Neon Dashboard > Connection Details 取得
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Webhook Configuration
# 部署後的公開網址（必須是 HTTPS）
# 範例:
#   - Zeabur: https://your-app.zeabur.app
#   - 本地測試: 使用 ngrok (https://abc123.ngrok.io) 或其他隧道工具
# 注意: 本地測試時不要使用 http://localhost:8000，Telegram API 不支援 HTTP 或 localhost
WEBHOOK_URL=https://your-app.zeabur.app

# Server Configuration (可選)
# Zeabur 會自動設定 PORT，通常不需要手動指定
PORT=8000
